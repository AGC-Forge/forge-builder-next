import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Demos",
  description: "View live demos of how to use SnapLand.",
};

export default function Demos() {
  return (
    <div>
      <h1>Demos</h1>
    </div>
  );
}
