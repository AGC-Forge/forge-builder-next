import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
  description: "Read our latest articles, news, and tips.",
};

export default function Blog() {
  return (
    <div>
      <h1>Blog</h1>
    </div>
  );
}
